name: sample_app
description: Fixture Flutter app for pubspec_scanner tests.
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  google_mobile_ads: ^5.0.0
  firebase_core: ^3.0.0
  purchases_flutter: ^8.0.0
  http: ^1.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
